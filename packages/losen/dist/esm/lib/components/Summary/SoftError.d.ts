import React from "react";
import { RenderableNode } from "../..";
type Props = {
    children: RenderableNode[];
};
export default function SoftError({ children }: Props): React.JSX.Element;
export {};
