import React from 'react';
import { RenderWithData } from 'losen';
export default function Radio({ property, heading, text, grid, options, disabled, clear, debug, setData, currentValue, data }: RenderWithData<"Radio">): React.JSX.Element;
