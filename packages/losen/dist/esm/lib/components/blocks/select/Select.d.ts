import React from 'react';
import { RenderWithData } from 'losen';
export default function Select({ setData, property, options, defaultOption, heading, currentValue, autocomplete, debug, data }: RenderWithData<"Select">): React.JSX.Element;
