import React from 'react';
import { RenderWithData } from "losen";
export default function Text({ debug, heading, id, text, warning, printonly, printhide, }: RenderWithData<"Text">): React.JSX.Element;
