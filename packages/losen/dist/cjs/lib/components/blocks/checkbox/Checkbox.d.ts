import React from "react";
import { RenderWithData } from "losen";
export default function Checkbox({ setData, heading, options, grid, disabled, property, debug, currentValue, data, update }: RenderWithData<"Checkbox">): React.JSX.Element;
