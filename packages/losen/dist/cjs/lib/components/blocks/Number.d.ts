import React from 'react';
import { RenderWithData } from 'losen';
export default function Number({ heading, property, errors, disabled, currentValue, placeholder, autocomplete, setData, maximum, minimum, step, unit, }: RenderWithData<"Number">): React.JSX.Element;
