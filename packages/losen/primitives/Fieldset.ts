import styled from "styled-components";

import injectStyles from "utils/inject-styles";

const Fieldset = injectStyles(styled.fieldset`
  border: 0;
  padding: 0;
  margin: 0;
`);

export default Fieldset;
