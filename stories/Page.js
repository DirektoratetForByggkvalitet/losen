import React from 'react';

const Page = () =>
  (<div>
    <h1>Page</h1>
    <p>
      A page is a type. A page has a heading. A page can have an array of children. Children are of
      the type Blocks. This is a list of the supported block:
    </p>
  </div>);

export default Page;
