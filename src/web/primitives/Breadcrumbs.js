import styled from 'styled-components';

import injectStyles from '../utils/inject-styles';

const Breadcrumbs = injectStyles(styled.div`
  font-size: 12px;
  font-weight: normal;
  margin-bottom: 20px;
`);

export default Breadcrumbs;
